diff --git a/crates/cli/src/main.rs b/crates/cli/src/main.rs
index d9c976b..9a73761 100644
--- a/crates/cli/src/main.rs
+++ b/crates/cli/src/main.rs
@@ -27,7 +27,7 @@ use walkdir::WalkDir;
 
 use std::io::IsTerminal;
 
-const URL_PREFIX: [&'static str; 5] = ["zed://", "http://", "https://", "file://", "ssh://"];
+const URL_PREFIX: [&'static str; 5] = ["zedha://", "http://", "https://", "file://", "ssh://"];
 
 struct Detect;
 
@@ -44,21 +44,21 @@ trait InstalledApp {
 
 #[derive(Parser, Debug)]
 #[command(
-    name = "zed",
+    name = "zedha",
     disable_version_flag = true,
-    before_help = "The Zed CLI binary.
-This CLI is a separate binary that invokes Zed.
+    before_help = "The Zedha CLI binary.
+This CLI is a separate binary that invokes Zedha.
 
 Examples:
-    `zed`
-          Simply opens Zed
-    `zed --foreground`
+    `zedha`
+          Simply opens Zedha
+    `zedha --foreground`
           Runs in foreground (shows all logs)
-    `zed path-to-your-project`
-          Open your project in Zed
-    `zed -n path-to-file `
+    `zedha path-to-your-project`
+          Open your project in Zedha
+    `zedha -n path-to-file `
           Open file/folder in a new window",
-    after_help = "To read from stdin, append '-', e.g. 'ps axf | zed -'"
+    after_help = "To read from stdin, append '-', e.g. 'ps axf | zedha -'"
 )]
 struct Args {
     /// Wait for all of the given paths to be opened/closed before exiting.
diff --git a/crates/client/src/client.rs b/crates/client/src/client.rs
index 2c8dfa8..5ed11a4 100644
--- a/crates/client/src/client.rs
+++ b/crates/client/src/client.rs
@@ -1936,8 +1936,8 @@ impl ProtoClient for Client {
     }
 }
 
-/// prefix for the zed:// url scheme
-pub const ZED_URL_SCHEME: &str = "zed";
+/// Prefix for the zedha:// URL scheme.
+pub const ZED_URL_SCHEME: &str = "zedha";
 
 /// A parsed Zed link that can be handled internally by the application.
 #[derive(Debug, Clone, PartialEq, Eq)]
diff --git a/crates/client/src/zed_urls.rs b/crates/client/src/zed_urls.rs
index 70ff953..163ebb3 100644
--- a/crates/client/src/zed_urls.rs
+++ b/crates/client/src/zed_urls.rs
@@ -7,7 +7,7 @@
 use gpui::App;
 use settings::Settings;
 
-use crate::ClientSettings;
+use crate::{ClientSettings, ZED_URL_SCHEME};
 
 fn server_url(cx: &App) -> &str {
     &ClientSettings::get_global(cx).server_url
@@ -65,5 +65,5 @@ pub fn acp_registry_blog(cx: &App) -> String {
 }
 
 pub fn shared_agent_thread_url(session_id: &str) -> String {
-    format!("zed://agent/shared/{}", session_id)
+    format!("{ZED_URL_SCHEME}://agent/shared/{session_id}")
 }
diff --git a/crates/install_cli/src/install_cli_binary.rs b/crates/install_cli/src/install_cli_binary.rs
index ee8b8b2..a912718 100644
--- a/crates/install_cli/src/install_cli_binary.rs
+++ b/crates/install_cli/src/install_cli_binary.rs
@@ -18,7 +18,7 @@ actions!(
 
 async fn install_script(cx: &AsyncApp) -> Result<PathBuf> {
     let cli_path = cx.update(|cx| cx.path_for_auxiliary_executable("cli"))?;
-    let link_path = Path::new("/usr/local/bin/zed");
+    let link_path = Path::new("/usr/local/bin/zedha");
     let bin_dir_path = link_path.parent().unwrap();
 
     // Don't re-create symlink if it points to the same CLI binary.
@@ -86,7 +86,7 @@ pub fn install_cli_binary(window: &mut Window, cx: &mut Context<Workspace>) {
                 Toast::new(
                     NotificationId::unique::<InstalledZedCli>(),
                     format!(
-                        "Installed `zed` to {}. You can launch {} from your terminal.",
+                        "Installed `zedha` to {}. You can launch {} from your terminal.",
                         path.to_string_lossy(),
                         ReleaseChannel::global(cx).display_name()
                     ),
@@ -97,5 +97,5 @@ pub fn install_cli_binary(window: &mut Window, cx: &mut Context<Workspace>) {
         register_zed_scheme(cx).await.log_err();
         Ok(())
     })
-    .detach_and_prompt_err("Error installing zed cli", window, cx, |_, _, _| None);
+    .detach_and_prompt_err("Error installing zedha cli", window, cx, |_, _, _| None);
 }
diff --git a/crates/install_cli/src/register_zed_scheme.rs b/crates/install_cli/src/register_zed_scheme.rs
index 048b2dd..47dbaea 100644
--- a/crates/install_cli/src/register_zed_scheme.rs
+++ b/crates/install_cli/src/register_zed_scheme.rs
@@ -4,7 +4,7 @@ use gpui::{AsyncApp, actions};
 actions!(
     cli,
     [
-        /// Registers the zed:// URL scheme handler.
+        /// Registers the Zedha URL scheme handler.
         RegisterZedScheme
     ]
 );
diff --git a/crates/paths/src/paths.rs b/crates/paths/src/paths.rs
index d222bd1..9311be5 100644
--- a/crates/paths/src/paths.rs
+++ b/crates/paths/src/paths.rs
@@ -15,7 +15,7 @@ pub const EDITORCONFIG_NAME: &str = ".editorconfig";
 /// and state directory paths.
 ///
 /// Forks should change this to avoid colliding with Zed's user data.
-pub const APP_NAME: &str = "Zed";
+pub const APP_NAME: &str = "Zedha";
 
 /// Lowercased form of [`APP_NAME`], for use in XDG-style paths on
 /// Linux/FreeBSD and the macOS `~/.config` fallback.
diff --git a/crates/release_channel/src/lib.rs b/crates/release_channel/src/lib.rs
index ca3aff6..efc0d1b 100644
--- a/crates/release_channel/src/lib.rs
+++ b/crates/release_channel/src/lib.rs
@@ -185,7 +185,7 @@ impl ReleaseChannel {
 
     /// Returns whether we want to poll for updates for this [`ReleaseChannel`]
     pub fn poll_for_updates(&self) -> bool {
-        !matches!(self, ReleaseChannel::Dev)
+        matches!(self, ReleaseChannel::Nightly | ReleaseChannel::Preview)
     }
 
     /// Returns the display name for this [`ReleaseChannel`].
@@ -194,7 +194,7 @@ impl ReleaseChannel {
             ReleaseChannel::Dev => "Zed Dev",
             ReleaseChannel::Nightly => "Zed Nightly",
             ReleaseChannel::Preview => "Zed Preview",
-            ReleaseChannel::Stable => "Zed",
+            ReleaseChannel::Stable => "Zedha",
         }
     }
 
@@ -204,7 +204,7 @@ impl ReleaseChannel {
             ReleaseChannel::Dev => "dev",
             ReleaseChannel::Nightly => "nightly",
             ReleaseChannel::Preview => "preview",
-            ReleaseChannel::Stable => "stable",
+            ReleaseChannel::Stable => "zedha",
         }
     }
 
@@ -216,7 +216,7 @@ impl ReleaseChannel {
             ReleaseChannel::Dev => "dev.zed.Zed-Dev",
             ReleaseChannel::Nightly => "dev.zed.Zed-Nightly",
             ReleaseChannel::Preview => "dev.zed.Zed-Preview",
-            ReleaseChannel::Stable => "dev.zed.Zed",
+            ReleaseChannel::Stable => "me.ghostwriternr.Zedha",
         }
     }
 
diff --git a/crates/zed/Cargo.toml b/crates/zed/Cargo.toml
index 4f7ec30..2355c10 100644
--- a/crates/zed/Cargo.toml
+++ b/crates/zed/Cargo.toml
@@ -299,11 +299,11 @@ osx_url_schemes = ["zed"]
 
 [package.metadata.bundle-stable]
 icon = ["resources/app-icon@2x.png", "resources/app-icon.png"]
-identifier = "dev.zed.Zed"
-name = "Zed"
+identifier = "me.ghostwriternr.Zedha"
+name = "Zedha"
 osx_minimum_system_version = "10.15.7"
 osx_info_plist_exts = ["resources/info/*"]
-osx_url_schemes = ["zed"]
+osx_url_schemes = ["zedha"]
 
 [package.metadata.cargo-machete]
 ignored = ["profiling", "zstd", "tracing"]
